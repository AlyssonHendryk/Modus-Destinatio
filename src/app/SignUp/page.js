"use client"
import { useState } from "react"
import { useRouter } from "next/navigation"
import { useApp } from "@/context/AppContext"
export default function SignUp(){const[role,setRole]=useState("admin");const[form,setForm]=useState({name:"",email:"",password:"",confirm:""});const[error,setError]=useState("");const router=useRouter();const{register}=useApp();function submit(e){e.preventDefault();if(!form.name.trim()||!form.email.trim()||!form.password)return setError("Preencha todos os campos.");if(form.password.length<6)return setError("A senha deve ter ao menos 6 caracteres.");if(form.password!==form.confirm)return setError("As senhas não coincidem.");const r=register({name:form.name,email:form.email,password:form.password,type:role});if(!r.ok)return setError(r.message);router.push("/dashboard")};return <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-purple-600 via-pink-500 to-orange-500 p-4"><div className="bg-white w-[420px] rounded-2xl overflow-hidden shadow-2xl"><div className="bg-gradient-to-r from-purple-600 to-orange-500 text-white text-center py-8 px-6"><h1 className="text-3xl font-bold">Criar Conta</h1><p className="text-sm mt-2 opacity-90">Preencha os dados para começar</p></div><form onSubmit={submit} className="p-8 space-y-4"><div className="flex gap-3"><button type="button" onClick={()=>setRole("admin")} className={`flex-1 py-3 rounded-xl ${role==="admin"?"bg-purple-600 text-white":"bg-gray-200 text-gray-600"}`}>Administrador</button><button type="button" onClick={()=>setRole("employee")} className={`flex-1 py-3 rounded-xl ${role==="employee"?"bg-orange-500 text-white":"bg-gray-200 text-gray-600"}`}>Funcionário</button></div>{[["name","Nome Completo","text"],["email","Email","email"],["password","Senha","password"],["confirm","Confirmar Senha","password"]].map(([key,label,type])=><label key={key} className="block text-sm font-medium text-gray-700">{label}<input type={type} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})} className="mt-1 w-full border rounded-xl p-3 text-gray-800"/></label>)}{error&&<p className="text-sm text-red-600 bg-red-50 border border-red-200 rounded-xl p-3">{error}</p>}<button className="w-full bg-gradient-to-r from-purple-600 to-pink-500 text-white py-3 rounded-xl font-medium">Cadastrar</button><button type="button" onClick={()=>router.push("/login")} className="w-full text-gray-600 text-sm">← Voltar para login</button></form></div></div>}
