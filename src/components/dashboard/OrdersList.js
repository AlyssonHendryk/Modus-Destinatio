"use client"
import { CheckCircle, XCircle, Clock, Eye, Truck, PackageSearch } from "lucide-react"
import { formatCurrency, ORDER_STATUS } from "@/utils/formatters"
export default function OrdersList({ orders = [], onOpenOrders }) {
  const cfg={completed:[CheckCircle,"text-green-600"],cancelled:[XCircle,"text-red-600"],waiting:[Clock,"text-purple-600"],shipped:[Truck,"text-orange-600"],separating:[PackageSearch,"text-amber-600"]}
  return <div className="bg-white rounded-2xl p-6 shadow-sm border border-gray-100"><div className="flex justify-between items-center mb-5"><div><h2 className="font-semibold text-gray-900">Pedidos recentes</h2><p className="text-sm text-gray-500">Dados compartilhados com a tela de pedidos</p></div><button onClick={onOpenOrders} className="text-sm text-purple-600 hover:underline">Ver todos</button></div><div className="space-y-2">{orders.slice(0,5).map(o=>{const [Icon,color]=cfg[o.status]||[Clock,"text-gray-600"];return <div key={o.id} className="flex items-center justify-between p-3 rounded-xl hover:bg-gray-50"><div className="flex items-center gap-3"><Icon className={`w-5 h-5 ${color}`}/><div><p className="text-sm font-medium text-gray-900">{o.id} • {o.customer}</p><p className="text-xs text-gray-500">{ORDER_STATUS[o.status]||o.status} • {o.date}</p></div></div><div className="flex items-center gap-3"><span className="font-medium text-gray-900">{formatCurrency(o.total)}</span><Eye className="w-4 h-4 text-gray-400"/></div></div>})}{orders.length===0&&<p className="text-sm text-gray-500 py-6 text-center">Nenhum pedido encontrado.</p>}</div></div>
}
