"use client"
import { useState } from "react"
import { Check, Plus, Trash2 } from "lucide-react"
import { useApp } from "@/context/AppContext"
export default function TasksPanel(){const{tasks,addTask,toggleTask,deleteTask}=useApp();const[title,setTitle]=useState("");function submit(e){e.preventDefault();addTask(title);setTitle("")}return <div className="bg-white rounded-2xl p-6 shadow-sm border border-gray-100"><h2 className="font-semibold text-gray-900 mb-1">Tarefas</h2><p className="text-sm text-gray-500 mb-4">Pendências operacionais</p><form onSubmit={submit} className="flex gap-2 mb-4"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Nova tarefa" className="flex-1 min-w-0 border rounded-lg px-3 py-2 text-sm text-gray-900"/><button className="p-2 bg-purple-600 text-white rounded-lg"><Plus className="w-4 h-4"/></button></form><div className="space-y-2">{tasks.map(t=><div key={t.id} className="flex items-center gap-2 p-2 rounded-lg hover:bg-gray-50"><button onClick={()=>toggleTask(t.id)} className={`w-6 h-6 rounded-md border flex items-center justify-center ${t.done?"bg-green-500 border-green-500 text-white":"border-gray-300"}`}>{t.done&&<Check className="w-4 h-4"/>}</button><span className={`flex-1 text-sm ${t.done?"line-through text-gray-400":"text-gray-700"}`}>{t.title}</span><button onClick={()=>deleteTask(t.id)} className="text-red-500 p-1"><Trash2 className="w-4 h-4"/></button></div>)}</div></div>}
